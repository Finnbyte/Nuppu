data $echofmts = { b "%s\n", b 0 }
data $echofmtd = { b "%d\n", b 0 }
data $fmtd = { b "%d", b 0 }
data $fmts = { b "%s", b 0 }

function l __$add(l %x, l %c) {
	@start
	%o =l add %x, %c
	ret %o
}

function l __$sub(l %x, l %c) {
	@start
	%o =l sub %x, %c
	ret %o
}

function l __$mul(l %x, l %c) {
	@start
	%o =l mul %x, %c
	ret %o
}

function l __$div(l %x, l %c) {
	@start
	%o =l div %x, %c
	ret %o
}

function l __$mod(l %x, l %c) {
	@start
	%o =l rem %x, %c
	ret %o
}

function l $__itoa(l %X0) {
	@start
		%X0L =l loadl %X0
		call $sprintf(l %X0, l $fmtd, l %X0L)
		ret %X0
}

function $__echo_str(l %X0) {
	@start
		call $printf(l $echofmts, l %X0)
		ret
}

function $__echo_int(l %x) {
	@start
		call $printf(l $echofmtd, l %x)
		ret
}
